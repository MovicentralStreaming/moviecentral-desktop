interface SearchResult {
  title: string
  mediaType: string
  mediaId: string
  poster: string
}

interface Season {
  number: number
  id: string
}

interface Episode {
  number: number
  id: string
}

interface Server {
  name: string
  id: string
}

interface Source {
  link: string
}
